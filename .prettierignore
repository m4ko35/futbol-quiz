node_modules
.next
out
build
coverage
package-lock.json
prisma/migrations
scripts/etl/.cache
src/generated
*.db

# Strix (yerel güvenlik taraması) çıktısı — bizim kaynağımız değil, araç üretti.
strix_runs

# Dışarıdan gelen tasarım teslimatı — bizim kaynağımız değil, bir referans.
# Yeniden biçimlendirmek 2.274 satırı değiştirir ve sürümler arası
# karşılaştırmayı imkânsız kılar.
ARAYUZ-TASARIM.html

# TestSprite (uçtan uca test aracı) çıktısı — bizim kaynağımız değil, araç
# üretti: raporlar, test planları ve üretilen betikler. Biçimlendirmek anlamsız.
testsprite_tests

# Stitch tasarım teslimatları + arayüz yenileme brief/plan — referans, kaynak
# değil. Dışarıdan gelen HTML/MD; biçimlendirmek anlamsız.
tasarim
stitch-arayuz-brifi.md
